# 高德地图配置说明

网页版旅行地图已接入高德地图 JS API 2.0。真实 Key 和安全密钥只应存放在本地 `.env` 或部署平台环境变量中，不要写入源码或提交到 GitHub。

## 本地运行

1. 复制 `.env.example` 为 `.env`。
2. 填写高德开放平台创建的“Web 端（JS API）”Key 和配套安全密钥：

   ```properties
   AMAP_JS_KEY=你的Web端Key
   AMAP_SECURITY_JS_CODE=你的安全密钥
   AMAP_PROXY_ENABLED=true
   ```

3. 运行 `mvn spring-boot:run` 或 `start-app.bat`。
4. 打开 `http://localhost:8080/map`。

项目默认通过 Spring Boot 的 `/_AMapService` 同源代理使用安全密钥，浏览器 HTML 中不会出现安全密钥。

## Back4app 部署

在 Back4app 容器应用的 Environment Variables 中设置：

```text
AMAP_JS_KEY=你的Web端Key
AMAP_SECURITY_JS_CODE=你的安全密钥
AMAP_PROXY_ENABLED=true
```

重新部署后访问 `/map`。无需把密钥写入 Dockerfile，也无需额外配置 Nginx。

如果已经有独立的高德反向代理，可以额外设置：

```text
AMAP_SERVICE_HOST=https://你的域名/_AMapService
```

设置外部代理地址后，网页会优先使用该地址；服务器仍不会把安全密钥输出到页面。

## 常见问题

- 页面提示“高德地图尚未配置”：检查环境变量名称并重启应用。
- 地图加载失败：检查 Key 类型、安全密钥配对关系、高德控制台域名设置和服务器网络。
- 浏览器控制台出现 CSP 错误：确认运行的是最新构建，而不是旧的 `安装包/旅迹-网站服务.jar`。
- Android 应用通过 WebView 显示同一网页地图；微信小程序继续使用微信原生地图，不需要这组 Web Key。
